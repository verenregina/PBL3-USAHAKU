{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "env": {
            "NIXPACKS_PHP_VERSION": "8.4"
        }
    },
    "deploy": {
        "startCommand": "php artisan serve --host 0.0.0.0 --port ${PORT} --public"
    }
}